import React from 'react'
// import { infoContext } from '../context/InfoContext'

const Nav = () => {

        //       const {State} =  useContext(infoContext)


  return (
               <div className='w-full h-screen fixed py-2 px-1 flex text-black justify-end items-start top-0 z-[140] pointer-events-none '>
                      <div className='w-[10%] flex items-center justify-center h-[10%] rounded-lg bg-white' >
                              <p className='f3 text-lg'>Zero la</p>
                      </div>
      </div>  
  )
}

export default Nav